# Required — PostgreSQL connection string
DATABASE_URL=postgres://user:password@localhost:5432/expert_listing?sslmode=disable

# Port the HTTP server listens on (default: 8080)
PORT=8080

# Application environment: development | production
# In production mode, logs are formatted as JSON.
APP_ENV=development
